# PostgreSQL (e.g. docker compose up -d)
DATABASE_URL="postgresql://codetrust:codetrust@localhost:5432/codetrust"

# Optional: fetch PR files from GitHub
GITHUB_TOKEN=""

# Optional: second-pass review (structured JSON). Omit to use rules only.
# OPENAI_API_KEY=""
# OPENAI_MODEL="gpt-4o-mini"
# ENABLE_LLM="false"
